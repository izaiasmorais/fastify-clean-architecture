generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id       String @id
  name     String
  email    String @unique
  phone    BigInt
  document BigInt @unique
  password String
  role     String

  @@map("users")
}
